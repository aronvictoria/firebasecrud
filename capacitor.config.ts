import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firebase.firebaseCrud',
  appName: 'firebase-crud',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
